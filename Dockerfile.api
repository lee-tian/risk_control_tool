FROM node:22-alpine
WORKDIR /app

RUN apk add --no-cache python3 py3-pip \
  && python3 -m pip install --break-system-packages --no-cache-dir moomoo-api

COPY api ./api

EXPOSE 3001
CMD ["node", "api/server.mjs"]
